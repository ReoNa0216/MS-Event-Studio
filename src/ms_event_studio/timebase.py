"""Product import surface for the versioned shared MS core."""

from flame_ms_core.timebase import (
    NANOSECONDS_PER_SECOND,
    NANOSECONDS_PER_MINUTE,
    _decimal,
    minutes_to_ns,
    seconds_to_ns,
    AnalysisRange,
)
